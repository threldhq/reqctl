acceptance_criteria:
- given: the folder the numbered layout used holding one baseline file
  then: the product refuses the generation
  when: baseline --generate is run
- given: the folder the numbered layout used holding two baseline files
  then: the product refuses the generation
  when: baseline --generate is run
assessed: {}
priority: medium
relations: {}
status: approved
text: If a baseline would be generated while the corpus holds a baseline file of the
  numbered layout, then the product shall refuse the generation.
type: functional
verification: inspection
