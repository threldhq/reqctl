acceptance_criteria:
- given: a field the governing schema constrains to a date, holding a number another
    item states as a quantity
  then: the product accepts both items
  when: the corpus is validated
- given: a field the governing schema does not constrain to a date, holding a number
    another item states as a quantity
  then: the product refuses the item
  when: the corpus is validated
- given: a field the governing schema constrains to a date, holding a value that is
    not a date
  then: the product refuses the field
  when: the corpus is validated
assessed:
  REQ-31827606: 1tyZUHjYl_sV4i0yQaZ_ZicBMOyorn7fBbNxeU1cFRU
priority: medium
relations:
  REQ-31827606: constrains
status: approved
text: Where the governing schema constrains a field to a date, the product shall exclude
  that field from every check that reads an item's quantities.
type: functional
verification: inspection
