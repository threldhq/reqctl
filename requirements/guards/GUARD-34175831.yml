acceptance_criteria:
- given: a repository holding no plugin marketplace
  then: the build refuses the change
  when: the build runs
- given: a reqctl wheel stating no project name
  then: the build refuses the change
  when: the build runs
- given: a plugin marketplace listing the plugin under a name other than the project
    name the reqctl wheel states
  then: the build refuses the change
  when: the build runs
assessed:
  GUARD-56838909: thIjmFd7Wvy9mCXKuc9YQFNqxbnPNTu0ZrBVmxzAo3w
relations:
  GUARD-56838909: constrains
status: approved
text: If the plugin marketplace of the repository lists no plugin under the project
  name the reqctl wheel states, then the build shall refuse the change.
