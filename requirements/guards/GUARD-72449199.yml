acceptance_criteria:
- given: an allowlist admitting the stamp shape in a file outside the [corpus root](corpus_root)
  then: the build refuses the change
  when: the build runs
- given: an allowlist admitting the stamp shape that states no paths
  then: the build refuses the change
  when: the build runs
assessed:
  corpus_root: 7CziDMdjMvRZz0Ca6kiRo5TWAjuy2HNRNmLXRny6eM0
relations: {}
status: approved
text: If the secret scan admits the stamp of an item or an entry in a file outside
  the [corpus root](corpus_root), then the build shall refuse the change.
