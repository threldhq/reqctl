acceptance_criteria:
- given: a pattern for stamps that misses the stamp reqctl writes for a requirement,
    a parameter, a term or an entry
  then: the build refuses the change
  when: the build runs
- given: an allowlist for stamps whose paths admit no file under the [corpus root](corpus_root)
  then: the build refuses the change
  when: the build runs
- given: an allowlist for stamps whose pattern is matched against the line rather
    than the value the scan captured
  then: the build refuses the change
  when: the build runs
assessed:
  corpus_root: 7CziDMdjMvRZz0Ca6kiRo5TWAjuy2HNRNmLXRny6eM0
relations: {}
status: draft
text: If the secret scan would report the stamp of an item or an entry in a file under
  the [corpus root](corpus_root) as a secret, then the build shall refuse the change.
